{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "buildandinstall",
            "type": "shell",
            "command": "cmake --build ${workspaceRoot}/build --config Debug --target install -DCMAKE_PREFIX_PATH=${env:HOME}/anaconda3/envs/gadgetron-starter",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}